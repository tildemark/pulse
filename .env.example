# .env.example

# ----------------------------------------------------------------------------------
# CRITICAL: HOSTING & ACCESS
# ----------------------------------------------------------------------------------
# The public URL where P.U.L.S.E. will be accessible. Used to generate badge and JS

# script URLs correctly. MUST include the protocol (http:// or https://).
DOMAIN=https://www.google.com/search?q=https://counter.sanchez.com

# URL to your GitHub repository. Used in the public dashboard footer for visibility.
GITHUB_URL=https://www.google.com/search?q=https://github.com/tildemark/pulse

# ----------------------------------------------------------------------------------
# SECURITY: ADMIN BASIC AUTH
# ----------------------------------------------------------------------------------

# Username for the dashboard access (Basic Auth).
ADMIN_USER=myadmin

# Password for the dashboard access (Basic Auth).
ADMIN_PASS=ChangeThisSecurePassword123!

# ----------------------------------------------------------------------------------
# INTERNAL: DOCKER NETWORK
# ----------------------------------------------------------------------------------
# Hostname for the Redis service within the Docker network.

# Do NOT change this unless you modify the docker-compose.yml service name.
REDIS_HOST=redis
